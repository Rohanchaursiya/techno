import 'package:flutter/material.dart';

class Net_HARD extends StatelessWidget {
  const Net_HARD({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Net Hard"),
      ),
    );
  }
}
