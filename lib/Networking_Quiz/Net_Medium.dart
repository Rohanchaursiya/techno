import 'package:flutter/material.dart';

class Net_MediumM extends StatelessWidget {
  const Net_MediumM({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("NET MEDIUM"),
      ),
    );
  }
}
