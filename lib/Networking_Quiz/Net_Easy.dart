import 'package:flutter/material.dart';

class Net_EASY extends StatelessWidget {
  const Net_EASY({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Net Easy"),
      ),
    );
  }
}
