import 'package:flutter/material.dart';

class OS_Easy extends StatelessWidget {
  const OS_Easy({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("OS Easy"),
      ),
    );
  }
}
