import 'package:flutter/material.dart';

class OS_Hard extends StatelessWidget {
  const OS_Hard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("OS Hard"),
      ),
    );
  }
}
