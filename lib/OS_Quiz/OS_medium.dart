import 'package:flutter/material.dart';

class OS_Medium extends StatelessWidget {
  const OS_Medium({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("OS Medium"),
      ),
    );
  }
}
