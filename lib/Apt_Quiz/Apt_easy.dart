import 'package:flutter/material.dart';

class Apt_Easy extends StatelessWidget {
  const Apt_Easy({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Easy"),
      ),
    );
  }
}
