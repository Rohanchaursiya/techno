import 'package:flutter/material.dart';
class Apt_Hard extends StatelessWidget {
  const Apt_Hard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Hard"),
      ),

    );
  }
}
