import 'package:flutter/material.dart';

class Prog_Medium extends StatelessWidget {
  const Prog_Medium({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Prog Medium"),
      ),
    );
  }
}
