import 'package:flutter/material.dart';

class Prog_Easy extends StatelessWidget {
  const Prog_Easy({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Prog Easy"),
      ),
    );
  }
}
