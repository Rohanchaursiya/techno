import 'package:flutter/material.dart';

class Prog_Hard extends StatelessWidget {
  const Prog_Hard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Prog Hard"),
      ),
    );
  }
}
