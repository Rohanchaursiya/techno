import 'package:flutter/material.dart';

class Math_Medium extends StatelessWidget {
  const Math_Medium({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(" Math Medium"),
      ),
    );
  }
}
