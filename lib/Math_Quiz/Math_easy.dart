import 'package:flutter/material.dart';

class Math_Easy extends StatelessWidget {
  const Math_Easy({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(" MthEasy"),
      ),
    );
  }
}
