import 'package:flutter/material.dart';

class Math_Hard extends StatelessWidget {
  const Math_Hard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(" Math Hard"),
      ),
    );
  }
}
